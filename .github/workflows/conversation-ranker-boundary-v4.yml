name: Conversation Ranker Boundary v4

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: "Type YES to spend exactly 20 TypeSafe/Jev requests for boundary calibration v4"
        required: true
        default: "NO"
  push:
    branches:
      - main
    paths:
      - .github/conversation-ranker-boundary-v4.trigger

permissions:
  contents: write

jobs:
  conversation-ranker-boundary-v4:
    if: ${{ github.event_name == 'push' || inputs.confirm == 'YES' }}
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install
        run: npm install --ignore-scripts --no-audit --no-fund

      - name: Run bounded multilingual ranker boundary experiment
        env:
          TYPESAFE_API_KEY: ${{ secrets.TYPESAFE_API_KEY }}
          JEV_ALLOW_LIVE: "1"
          JEV_LIVE_MAX_REQUESTS: "20"
          JEV_RELAY_URL: "https://jev-language-typesafe-relay.nolane-file.workers.dev"
        run: |
          npx tsx scripts/conversation-ranker-boundary-v4.ts > /tmp/conversation-ranker-boundary-v4.json
          cat /tmp/conversation-ranker-boundary-v4.json

      - name: Freeze sanitized v4 evidence
        run: |
          set -euo pipefail
          mkdir -p docs/evidence
          cp /tmp/conversation-ranker-boundary-v4.json docs/evidence/CONVERSATION-RANKER-BOUNDARY-v4.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/evidence/CONVERSATION-RANKER-BOUNDARY-v4.json
          git commit -m "chore: record conversation ranker boundary v4 evidence [skip ci]"
          git push origin HEAD:main
