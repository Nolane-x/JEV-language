# trace-replay

Status: **prototype / PARTIAL**.

Implemented in this wave: structured TraceEvent DAGs with parent validation and cycle rejection, configuration digests, versioned ReplayManifest, exact/live/partial replay mode vocabulary, deterministic replay-bundle hashing, and an in-memory trace recorder. Broader replay executors, source materialization policy, external-service live differential replay, benchmark bundle generation, and persistence remain open.
